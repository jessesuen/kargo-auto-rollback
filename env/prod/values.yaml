environment: prod

createNamespace: true

image:
  tag: 0.1.0

replicaCount: 1

ingress:
  enabled: true
  hosts:
    - host: auto-rollback-prod.demo.akuity.io
      paths:
        - path: /
          pathType: ImplementationSpecific

serviceMonitor:
  enabled: true
  interval: 15s
