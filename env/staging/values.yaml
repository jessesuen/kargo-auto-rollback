environment: staging

createNamespace: true

image:
  tag: 0.3.1

replicaCount: 2

ingress:
  enabled: true
  hosts:
    - host: auto-rollback-staging.demo.akuity.io
      paths:
        - path: /
          pathType: ImplementationSpecific

serviceMonitor:
  enabled: true
  interval: 15s
